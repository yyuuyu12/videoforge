# VideoForge 全流程自检报告

检查时间：2026-07-13（Asia/Singapore）
检查作品：Job #4

## 1. 画面与数字人安全区

- 重新生成已完成，作品停在 `gate_chapters / waiting_approval`。
- 5 个章节全部通过安全区自动检查。
- 右侧讲师区按 1920px 舞台预留约 640px；开场使用独立 640px 网格列，其余章节使用 700px 右侧内边距。
- 浏览器实测章节内容区域右边界与舞台右边界之间存在完整安全区。
- TypeScript 检查通过，0 错误。

结论：通过。

## 2. 配音、字幕与播放

- 检测到 25 个 MP3 配音片段。
- 逐字字幕数据存在。
- 自动播放入口与浏览器声音解锁链路已调整：用户点击播放遮罩时直接调用当前音频的 `play()`。
- 浏览器自动化点击不一定被浏览器视为可信用户手势，因此最终声音响度仍需真人点击做一次听感确认。

结论：文件与代码检查通过；真人听感确认待做。

## 3. 数字人模型与口型

- 作品已启用右侧三分之一数字人布局。
- 素材库当前没有真人视频，作品内没有数字人源视频。
- HeyGem 配置地址为 `http://127.0.0.1:7861`，当前无服务监听，健康检查失败。
- `public/avatar/lipsync.mp4` 不存在。
- 当前生产代码仍是整段音频/整段视频单次调用，尚未完成约 10 秒分段、避开乒乓反转点、逐段拼接和交叉相关偏移计算，因此即使 HeyGem 恢复也不能直接判定满足最终质量标准。
- 当前无法执行真实嘴型、动作连续性、乒乓反转点和交叉相关偏移检查。
- 后端已增加 HeyGem 预检；服务未就绪时会明确失败，不再进入假生成状态。
- 后端已增加数字人百分比进度事件。

结论：未通过，阻塞于真人素材和 HeyGem 服务。

## 4. 右侧对话微调

- 已真实提交一次开场章节样式微调，API Agent 成功运行并通过 TypeScript 检查。
- 测试发现 Agent 越界修改了文案和数字人安全区内容。
- 已人工恢复数字人安全区为空，并将开场安全区修正为 640px。
- 已收紧反馈提示词：仅样式请求不得修改文案、步骤或结构；数字人安全区不得加入卡片、摘要或图表。
- 工作台现在显示每条反馈的“正在修改 / 修改完成 / 修改失败”状态。

结论：链路可用；首次范围控制失败，已修复约束，仍建议后续保留差异审查。

## 5. 长任务进度

- 画面生成、音频合成、字幕、数字人和导出阶段统一显示运行状态条。
- 重新生成按钮提交后立即显示状态并禁止重复点击。
- 数字人任务会记录模型返回的百分比进度。
- 已增加 `GET /api/jobs/:id/audit` 自动审计接口。

结论：通过。

## 自动审计结果

- 画面安全区：5/5 章节通过
- 配音：25 个片段，通过
- 数字人源视频：缺失
- HeyGem：离线
- 数字人成片：缺失
- 对话微调任务：1 次，已完成并复盘

## 完成数字人最终验收所需条件

1. 在素材库上传一段正面真人 MP4/MOV。
2. 启动 `127.0.0.1:7861` 的 HeyGem 服务并确保模型 ready。
3. 重新执行数字人生成。
4. 检查分段乒乓循环、反转点、逐步偏移、口型连续性和最终合成视频。
